# name : respond_to(extjs coll) ...
# key : resp
# group : rails
# --
respond_to do |format|
  format.json do
    render :json => { :success => true, :total => @${1:models}.length, :$2 => @$3.as_json(:only => [:id, ${4:other fields}]) }
  end
end
